{
  "name": "lambda-task-generator",
  "version": "1.0.0",
  "description": "Rookout task generator for todo demo app",
  "main": "task_generator.js",
  "author": "Mickael Alliel",
  "license": "ISC",
  "repository": "https://github.com/Rookout/lambda-task-generator",
  "scripts": {
    "install-rookout": "docker run -v `pwd`:`pwd` -w `pwd` -i -t lambci/lambda:build-nodejs10.x npm install --production"
  },
  "dependencies": {
    "rookout": "^0.12.0"
  }
}
